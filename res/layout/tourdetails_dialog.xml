<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >
     <LinearLayout
        android:id="@+id/scenic_background"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_login_border"
        android:orientation="vertical" 
         android:gravity="center">

        <ImageView
            android:id="@+id/tourdetails_dialog_imageView"
            android:layout_width="600dp"
            android:layout_height="500dp"
            android:src="@drawable/icon" 
            android:scaleType="fitXY"
            android:layout_gravity="center_horizontal"
            android:layout_margin="10dp">
        </ImageView>

        <TextView
            android:id="@+id/tourdetails_dialog_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center_horizontal"
            android:text="标题"
            android:textColor="#000000" 
            android:layout_marginTop="5dp"
            android:textSize="18sp"
            android:singleLine="true">
        </TextView>
        <Button 
     android:id="@+id/tourdetails_dialog_cancel"
      android:layout_width="200dp"
      android:layout_height="50dp"
      android:background="@drawable/bg_blue_style"
      android:layout_margin="10dp"
      android:text="返回"
      android:textColor="@color/white"
        android:textSize="18sp"
    />
    </LinearLayout> 

</LinearLayout>